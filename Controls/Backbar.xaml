<UserControl x:Class="NeuroApp.Controls.Backbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeuroApp.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Button x:Name="BackButton"
                Content="←"
                Height="40"
                Width="50"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Command="{Binding GoBackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Grid.Column="0"
                Click="BackButton_Click"/>

        <TextBlock Grid.Column="1" HorizontalAlignment="Center" FontSize="29" FontWeight="SemiBold" Text="Cockpit"/>
    </Grid>
</UserControl>
