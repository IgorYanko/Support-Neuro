<UserControl x:Class="NeuroApp.HomeScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeuroApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
            
            <Button x:Name="btnCockpit" Width="{Binding ResponsiveBigButtons.ButtonSize}"
                                        Height="{Binding ResponsiveBigButtons.ButtonSize}"
                                        Command="{Binding MainViewModel.ShowCockpitScreen_}"
                                        Content="Cockpit"
                                        Margin="10 0 10 0"
                                        FontSize="20"/>

            <Button x:Name="btnCustomers" Width="{Binding ResponsiveBigButtons.ButtonSize}" 
                                          Height="{Binding ResponsiveBigButtons.ButtonSize}"
                                          Command="{Binding MainViewModel.ShowCustomersScreen_}"
                                          Content="Clientes"
                                          Margin="10 0 10 0"
                                          FontSize="20"/>
        </StackPanel>   
    </Grid>
</UserControl>
