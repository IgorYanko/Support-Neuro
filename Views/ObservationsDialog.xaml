<Window x:Class="NeuroApp.Views.ObservationsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:NeuroApp.Views"
        xmlns:style="clr-namespace:NeuroApp"
        mc:Ignorable="d"
        Height="280" Width="380"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{x:Null}"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="NoResize">

    <materialDesign:Card UniformCornerRadius="15" Background="{DynamicResource MaterialDesignPaper}"
                         Margin="25" materialDesign:ShadowAssist.ShadowDepth="Depth4">
        <materialDesign:DialogHost CloseOnClickAway="False" x:Name="DialogHost">
            <StackPanel>
                <DockPanel VerticalAlignment="Top">

                    <TextBlock Text="Insira as observações:" 
                               FontWeight="DemiBold"
                               FontSize="18"
                               Margin="12 10 0 0"/>
                    
                    <Button HorizontalAlignment="Right" Margin="0 10 12 0" Height="20" Width="20" Style="{StaticResource RoundedButton}"
                            materialDesign:ButtonAssist.CornerRadius="50" x:Name="BtnExit" Click="BtnExit_Click">
                        <materialDesign:PackIcon Kind="Close" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                </DockPanel>

                

                <TextBox x:Name="ObservationTextBox"
                         HorizontalAlignment="Center"
                         Margin="5 20 5 0"
                         MinLines="6" 
                         Width="315"
                         AcceptsReturn="True"
                         BorderBrush="Black"
                         BorderThickness="0.4"/>

                <DockPanel>
                    <Button x:Name="AddObservationButton"
                        Content="Adicionar"
                        Width="110"
                        HorizontalAlignment="Left"
                        Height="35"
                        Style="{StaticResource DefaultButton}"
                        Margin="10 15 5 0" Click="AddObservationButton_Click"/>

                    <Button x:Name="RemoveObservationButton"
                            Content="Excluir"
                            Width="110"
                            HorizontalAlignment="Right"
                            Height="35"
                            Style="{StaticResource DefaultButton}"
                            Margin="5 15 10 0" Click="RemoveObservationButton_Click"/>
                </DockPanel>
            </StackPanel>
        </materialDesign:DialogHost>
    </materialDesign:Card>
</Window>
